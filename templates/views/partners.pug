extends ../layouts/default


block content
  main.content.partners
    h2.content-title Партнери
    .container
      -for(let i=0; i<Math.ceil(partners.length/2); i++)
        .row
          if partners[i]
            +partner(partners[i])
          if partners[i+1]
            +partner(partners[i+1])
